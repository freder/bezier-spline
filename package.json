{
  "name": "@freder/bezier-spline",
  "version": "0.0.0",
  "main": "dist/index.js",
  "dependencies": {
    "ramda": "^0.23.0"
  },
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "eslint-config-airbnb-base": "^11.1.1",
    "jest": "^19.0.2"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "yarn run test -- --watch --notify",
    "compile": "babel -d dist/ src/",
    "prepublish": "npm run compile"
  },
  "jest": {
    "roots": [
      "src",
      "__tests__"
    ],
    "globals": {}
  }
}
